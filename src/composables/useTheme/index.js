import colors from "./colors";
import fonts from "./fonts";
import icons from "./icons";
import layouts from "./layouts";

export const useTheme = () => {
  return { colors, fonts, icons, layouts };
};
